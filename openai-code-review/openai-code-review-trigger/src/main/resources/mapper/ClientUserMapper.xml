<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.alias.infrastructure.mapper.IClientUserRepository">

    <resultMap id="clientUserResultMap" type="com.alias.domain.model.ClientUser">
        <id column="id" property="id" javaType="java.util.UUID" jdbcType="OTHER"/>
        <result column="client_identifier" property="clientIdentifier" javaType="java.util.UUID" jdbcType="OTHER"/>
        <result column="github_token" property="githubToken" jdbcType="VARCHAR"/>
        <result column="openai_api_key" property="openaiApiKey" jdbcType="VARCHAR"/>
        <result column="encrypted" property="encrypted" jdbcType="BOOLEAN"/>
        <result column="metadata" property="metadata" jdbcType="OTHER"
                typeHandler="com.alias.infrastructure.typehandler.JsonbTypeHandler"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="clientUserColumns">
        id
        , client_identifier, github_token, openai_api_key, encrypted, metadata, created_at, updated_at
    </sql>

    <!-- Save or update client user -->
    <insert id="save" parameterType="com.alias.domain.model.ClientUser" useGeneratedKeys="false">
        INSERT INTO client_users (id, client_identifier, github_token, openai_api_key, encrypted, metadata, created_at,
                                  updated_at)
        VALUES (#{id, javaType=java.util.UUID, jdbcType=OTHER},
                #{clientIdentifier, javaType=java.util.UUID, jdbcType=OTHER},
                #{githubToken, jdbcType=VARCHAR},
                #{openaiApiKey, jdbcType=VARCHAR},
                #{encrypted, jdbcType=BOOLEAN},
                #{metadata, jdbcType=OTHER, typeHandler=com.alias.infrastructure.typehandler.JsonbTypeHandler},
                #{createdAt, jdbcType=TIMESTAMP},
                #{updatedAt, jdbcType=TIMESTAMP}) ON CONFLICT (client_identifier) DO
        UPDATE SET
            github_token = EXCLUDED.github_token,
            openai_api_key = EXCLUDED.openai_api_key,
            encrypted = EXCLUDED.encrypted,
            metadata = EXCLUDED.metadata,
            updated_at = EXCLUDED.updated_at
    </insert>

    <!-- Find by ID -->
    <select id="findById" parameterType="java.util.UUID" resultMap="clientUserResultMap">
        SELECT
        <include refid="clientUserColumns"/>
        FROM client_users
        WHERE id = #{id, javaType=java.util.UUID, jdbcType=OTHER}
    </select>

    <!-- Find by client identifier -->
    <select id="findByClientIdentifier" parameterType="java.util.UUID" resultMap="clientUserResultMap">
        SELECT
        <include refid="clientUserColumns"/>
        FROM client_users
        WHERE client_identifier = #{clientIdentifier, javaType=java.util.UUID, jdbcType=OTHER}
    </select>

    <!-- Delete by ID -->
    <delete id="deleteById" parameterType="java.util.UUID">
        DELETE
        FROM client_users
        WHERE id = #{id, javaType=java.util.UUID, jdbcType=OTHER}
    </delete>

    <!-- Delete by client identifier -->
    <delete id="deleteByClientIdentifier" parameterType="java.util.UUID">
        DELETE
        FROM client_users
        WHERE client_identifier = #{clientIdentifier, javaType=java.util.UUID, jdbcType=OTHER}
    </delete>

    <!-- Check if exists by client identifier -->
    <select id="existsByClientIdentifier" parameterType="java.util.UUID" resultType="boolean">
        SELECT EXISTS(SELECT 1
                      FROM client_users
                      WHERE client_identifier = #{clientIdentifier, javaType=java.util.UUID, jdbcType=OTHER})
    </select>

</mapper>
