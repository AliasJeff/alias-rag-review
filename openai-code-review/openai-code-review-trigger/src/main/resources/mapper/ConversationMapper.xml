<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.alias.infrastructure.mapper.IConversationRepository">

    <resultMap id="conversationResultMap" type="com.alias.domain.model.Conversation">
        <id column="id" property="id" javaType="java.util.UUID" jdbcType="OTHER"/>
        <result column="client_identifier" property="clientIdentifier" javaType="java.util.UUID" jdbcType="OTHER"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="pr_url" property="prUrl" jdbcType="VARCHAR"/>
        <result column="repo" property="repo" jdbcType="VARCHAR"/>
        <result column="pr_number" property="prNumber" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="metadata" property="metadata" jdbcType="OTHER"
                typeHandler="com.alias.infrastructure.typehandler.JsonbTypeHandler"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
        <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="conversationColumns">
        id
        , client_identifier, title, pr_url, repo, pr_number, status, metadata, created_at, updated_at
    </sql>

    <!-- Save or update conversation -->
    <insert id="save" parameterType="com.alias.domain.model.Conversation" useGeneratedKeys="false">
        INSERT INTO conversations (id, client_identifier, title, pr_url, repo, pr_number, status, metadata, created_at,
                                   updated_at)
        VALUES (#{id, javaType=java.util.UUID, jdbcType=OTHER},
                #{clientIdentifier, javaType=java.util.UUID, jdbcType=OTHER},
                #{title, jdbcType=VARCHAR},
                #{prUrl, jdbcType=VARCHAR},
                #{repo, jdbcType=VARCHAR},
                #{prNumber, jdbcType=INTEGER},
                #{status, jdbcType=VARCHAR},
                #{metadata, jdbcType=OTHER, typeHandler=com.alias.infrastructure.typehandler.JsonbTypeHandler},
                #{createdAt, jdbcType=TIMESTAMP},
                #{updatedAt, jdbcType=TIMESTAMP}) ON CONFLICT (id) DO
        UPDATE SET
            title = EXCLUDED.title,
            pr_url = EXCLUDED.pr_url,
            repo = EXCLUDED.repo,
            pr_number = EXCLUDED.pr_number,
            status = EXCLUDED.status,
            metadata = EXCLUDED.metadata,
            updated_at = EXCLUDED.updated_at
    </insert>

    <!-- Find by ID -->
    <select id="findById" parameterType="java.util.UUID" resultMap="conversationResultMap">
        SELECT
        <include refid="conversationColumns"/>
        FROM conversations
        WHERE id = #{id, javaType=java.util.UUID, jdbcType=OTHER}
    </select>

    <!-- Find by client identifier -->
    <select id="findByClientIdentifier" parameterType="java.util.UUID" resultMap="conversationResultMap">
        SELECT
        <include refid="conversationColumns"/>
        FROM conversations
        WHERE client_identifier = #{clientIdentifier, javaType=java.util.UUID, jdbcType=OTHER}
        ORDER BY created_at DESC
    </select>

    <!-- Find by PR URL -->
    <select id="findByPrUrl" parameterType="java.lang.String" resultMap="conversationResultMap">
        SELECT
        <include refid="conversationColumns"/>
        FROM conversations
        WHERE pr_url LIKE #{prUrl, jdbcType=VARCHAR}
        ORDER BY created_at DESC
    </select>

    <!-- Find by status -->
    <select id="findByStatus" parameterType="java.lang.String" resultMap="conversationResultMap">
        SELECT
        <include refid="conversationColumns"/>
        FROM conversations
        WHERE status = #{status, jdbcType=VARCHAR}
        ORDER BY created_at DESC
    </select>

    <!-- Delete by ID -->
    <delete id="deleteById" parameterType="java.util.UUID">
        DELETE
        FROM conversations
        WHERE id = #{id, javaType=java.util.UUID, jdbcType=OTHER}
    </delete>

    <!-- Delete by client identifier -->
    <delete id="deleteByClientIdentifier" parameterType="java.util.UUID">
        DELETE
        FROM conversations
        WHERE client_identifier = #{clientIdentifier, javaType=java.util.UUID, jdbcType=OTHER}
    </delete>

    <!-- Count by client identifier -->
    <select id="countByClientIdentifier" parameterType="java.util.UUID" resultType="long">
        SELECT COUNT(*)
        FROM conversations
        WHERE client_identifier = #{clientIdentifier, javaType=java.util.UUID, jdbcType=OTHER}
    </select>

</mapper>
